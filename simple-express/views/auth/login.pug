extends ../_layouts/master

block content
  h1 登入表單
  form.form(id="loginFrm", method="POST", action="/auth/login")
    div
      label(for="email") 帳號:
      input#email(name="email", type="mail", value="1234@test.com")
    div
      label(for="password") 密碼:
      input#email(name="password", type="password",  value="1234")
      
    button(type="submit") 送出